---
- name: Récupérer services
  service_facts:

- name: Afficher état services
  debug:
    msg: "{{ item }} : {{ ansible_facts.services[item + '.service'].state | default('absent') }}"
  loop:
    - ssh
    - ufw
    - fail2ban
    - auditd
    - apparmor
    - clamav-freshclam
  changed_when: false
